#include <iostream>

using namespace std;

int main()
{
    char a='q';
    cout << a << endl;
    cout << sizeof(a) << " byte" << endl; //untuk menampilkan besar tipe data //aslinya 1-bit tetapi memori tidak mengenal istilah bit istilah yang digunakan adalah byte
    cin.get();
    return 0;
}
